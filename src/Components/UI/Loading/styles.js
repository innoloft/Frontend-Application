import { colors } from "../../../Theme/Colors";

const styles = {
    root: {
        position: 'fixed',
        top: 'calc(50% - 45px)',
        left: 'calc(50% - 45px)',
    },
    color: colors.secondary,
};
export default styles;