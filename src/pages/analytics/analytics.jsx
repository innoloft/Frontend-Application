import React from 'react'
import { withRouter } from 'react-router-dom'

function Analytics() {
    return (
        <section>
            <h1>This is the component for the analytics route.</h1>
        </section>
    )
}

export default withRouter(Analytics)