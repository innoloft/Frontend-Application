import React from 'react';
import { RouteComponentProps } from '@reach/router';

export const Home = (props: RouteComponentProps) => {
	return (
		<div>
			<h1 className='loading'>Welcome aboard!</h1>
		</div>
	);
};
