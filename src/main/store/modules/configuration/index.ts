import { FetchConfigrationStateDef } from "./types";

export const configuration: FetchConfigrationStateDef = {
    error: null,
    status: "rest",
    configuration: {},
};

export default configuration;
