import React from "react";
import { Header, Navigation, Main } from "../components";

const Defaultlayout = () => {
  return (
    <section>
      <Header />
      <Navigation />
      <Main />
    </section>
  );
};

export default Defaultlayout;
